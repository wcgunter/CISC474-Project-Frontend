image: node:8.12.0

pages:
  cache:
    paths:
    - node_modules/

  stage: deploy
  script:
  - npm install -g @angular/cli@14.2.7
  - npm install
  - ng build
  - mkdir -p public
  - mv dist/* public/
  artifacts:
    paths:
    - public
  only:
  - main
  - pages